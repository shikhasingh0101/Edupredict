# EduPredict Viva Questions

Prepare answers for:
- Why this dataset?
- Why regression?
- How did you handle missing values?
- Why Pipeline and ColumnTransformer?
- Why OneHotEncoder?
- Why scaling?
- Why do tree models not require scaling?
- What is target leakage?
- What is preprocessing leakage?
- Why feature engineering?
- How were engineered features selected?
- Why feature selection?
- Why PCA?
- How was the PCA component count selected?
- What is explained variance?
- Why might PCA reduce interpretability?
- Why MAE?
- Why RMSE?
- Why R²?
- Why cross-validation?
- Why hyperparameter tuning?
- Why MLflow?
- Why model registry?
- Why DVC?
- Why FastAPI?
- Why should Streamlit call FastAPI?
- Why Docker?
- Why GitHub Actions?
- What is data drift?
- What is model drift?
- What is concept drift?
- What is training-serving skew?
- How does retraining work?
- How is a champion model promoted?
- How is the model tested?
- How is reproducibility maintained?

Answers must be updated to match actual implementation and results.
